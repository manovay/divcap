*.csv.gz
*.parquet
*.json
*.jsonl
.env
__pycache__/
 
# Committed reference samples. Small, hand-picked slices of the gitignored
# data files so the record shape is visible without running the pullers or
# holding credentials. Negation must come after the pattern it overrides.
!sample_*.jsonl
!sample_*.json

# Machine-readable cross-sectional report artifacts are committed with results.
!m2_cross_sectional/results/data/report_metrics.json
!m2_cross_sectional/results/data/section_insights.json
